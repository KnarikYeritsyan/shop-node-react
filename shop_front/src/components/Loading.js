import React from 'react';

function Loading(props) {
    return (
        <div style={{width: '90%', textAlign: "center"}}>
            <h2 style={{ fontSize: '40px'}}>Lading...</h2>
        </div>
    );
}

export default Loading;